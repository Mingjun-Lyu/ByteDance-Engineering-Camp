import { useRouteMatching } from './useRouteMatching';
import { useRouteNavigation } from './useRouteNavigation';

// 导出所有hooks
export {
  useRouteMatching,
  useRouteNavigation
};

export default {
  useRouteMatching,
  useRouteNavigation
};